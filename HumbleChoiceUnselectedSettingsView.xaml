<UserControl x:Class="HumbleChoiceUnselected.HumbleChoiceUnselectedSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel>
        <TextBlock Text="humblebundle.com _simpleauth_sess cookie value:"/>
        <TextBox Text="{Binding Settings.Cookie}"/>
        <CheckBox Name="ImportEntitlements" IsChecked="{Binding Settings.ImportEntitlements}" Content="Import unredeemed Games from other purchases and bundles"/>
    </StackPanel>
</UserControl>
